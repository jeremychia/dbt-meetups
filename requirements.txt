# Direct dependencies of pipeline/*.py and dashboard/*.py.
# Install with: pip install -r requirements.txt && python -m playwright install chromium
#
# playwright   - scrape.py (headless Chromium against Meetup)
# numpy        - build_world_map.py, build_dashboard_data.py (aggregation)
# shapely      - build_world_map.py (polygon simplification/antimeridian handling)
#
# enrich.py has no Python package dependency: it shells out to an
# already-authenticated `claude` CLI (see pipeline/README.md).
playwright==1.61.0
numpy==2.5.1
shapely==2.1.2
